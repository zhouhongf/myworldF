import { AssetPathPipe } from './asset-path.pipe';

describe('AssetPathPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetPathPipe();
    expect(pipe).toBeTruthy();
  });
});
