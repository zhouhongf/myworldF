import { WidToNamePipe } from './wid-to-name.pipe';

describe('WidToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WidToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
