import { SplitLastPipe } from './split-last.pipe';

describe('SplitLastPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitLastPipe();
    expect(pipe).toBeTruthy();
  });
});
