import { AddDomainPipe } from './add-domain.pipe';

describe('AddDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new AddDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
