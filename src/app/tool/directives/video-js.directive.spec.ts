import { VideoJsDirective } from './video-js.directive';

describe('VideoJsDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoJsDirective();
    expect(directive).toBeTruthy();
  });
});
